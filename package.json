{
  "name": "talentscope-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "npm --workspace apps/api start",
    "dev": "npm --workspace apps/api run dev",
    "ingestion:start": "npm --workspace apps/ingestion start"
  }
}
